# Secrets — never commit these
backend/.env

# Python
__pycache__/
*.pyc
*.pyo
backend/venv/
venv/
.pytest_cache/
*.egg-info/

# Data (SQLite DB, PyTorch weights, logs) — lives outside the repo via
# SCHOLARPI_DATA_DIR / a mounted volume in production
*.db
*.db-wal
*.db-shm
*.pt
logs/

# OS / editor
.DS_Store
.vscode/
.idea/
